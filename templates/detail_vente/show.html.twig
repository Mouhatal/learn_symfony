{% extends 'base.html.twig' %}

{% block title %}DetailVente{% endblock %}

{% block body %}
    <h1>DetailVente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_vente.id }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ detail_vente.quantité }}</td>
            </tr>
            <tr>
                <th>Pu</th>
                <td>{{ detail_vente.pu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_vente_index') }}">back to list</a>

    <a href="{{ path('detail_vente_edit', {'id': detail_vente.id}) }}">edit</a>

    {{ include('detail_vente/_delete_form.html.twig') }}
{% endblock %}
